.shadow-sm.text-center.border-top.mb-4
  - if user.avatar.attached?
    .avatar-container
      = image_tag(user.avatar.variant(resize_to_limit: [300,300]), class: 'avatar-img')
  .card-body
    %h5.card-title= user.name
    %p.card-text= user.about
    = yield
